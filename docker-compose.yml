# docker-compose.yml

version: "3.4"

services:
  spring-app:
    image: spring-app
  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - /etc/nginx/nginx.conf:/etc/nginx/nginx.conf
      - /var/log/nginx:/var/log/nginx